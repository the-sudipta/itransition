# Enable mod_rewrite
RewriteEngine On

# -------------------------------------------
# ✅ Allow root path (project base) to load index.php
# -------------------------------------------
RewriteCond %{REQUEST_URI} ^/itransition/?$
RewriteRule ^.*$ - [L]

# -------------------------------------------
# 🛡️ Redirect any other direct folder access to 404
# -------------------------------------------
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^.*$ /itransition/view/error/_404_not_found_error.php [L]

# -------------------------------------------
# 🚫 Prevent directory listing
# -------------------------------------------
Options -Indexes

# -------------------------------------------
# ❌ Block direct access to sensitive files (optional)
# Uncomment and use if needed
# -------------------------------------------
#<FilesMatch "^(backend_utility_functions\.php|db_connect\.php|.*_Repo\.php)$">
#    Order Allow,Deny
#    Deny from all
#</FilesMatch>

# -------------------------------------------
# ⚠️ Default 404 Not Found handler
# -------------------------------------------
ErrorDocument 404 /itransition/view/error/_404_not_found_error.php
